{
  "name": "fastify-auth-skeleton",
  "version": "0.2.1",
  "description": "A skeleton project with typescript typeorm & jwt authentication",
  "main": "app.ts",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "clean": "npx rimraf build",
    "build": "tsc",
    "test": "",
    "dev": "npm run clean && tsc && concurrently -k -p \"[{name}]\" -n \"TypeScript,App\" -c \"yellow.bold,cyan.bold\" \"tsc -w\" \"fastify start --ignore-watch=.ts$ -w -l info -P build/app.js\"",
    "start": "npm run build && fastify start -l info build/app.js"
  },
  "keywords": [],
  "author": "SEK-Consulting",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "fastify": "^3.28.0",
    "fastify-autoload": "^3.13.0",
    "fastify-cli": "^2.15.0",
    "fastify-jwt": "^4.1.3",
    "fastify-plugin": "^3.0.1",
    "fastify-sensible": "^3.1.2",
    "mysql2": "^2.3.3",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.6"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/node": "^17.0.8",
    "concurrently": "^7.0.0",
    "fastify-tsconfig": "^1.0.1",
    "nodemon": "^2.0.15",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  }
}
